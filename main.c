#include "main.h"
#include <stdio.h>

/**
 * main - Entry Point
 * Return: 0 on success
 */

int main(void)
{
	int len;

	char *str = pisco";

	len = _printf
		("The number A : % d B : % i C : % d D : % i \n" , 'a' , 'b', "pisco" , str);

	printf("length : % d\n" , len);

	return (0);

}
